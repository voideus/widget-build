/// <reference types="react" />
import { ButtonProps } from '../../interfaces';
export declare function Button({ className, onClick, children, type, variant, }: ButtonProps): JSX.Element;
export default Button;
